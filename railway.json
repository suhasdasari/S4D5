{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd nerve-cord && npm ci"
  },
  "deploy": {
    "startCommand": "cd nerve-cord && node index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
